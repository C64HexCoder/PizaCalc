<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shelly.pizzacalc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pizza"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.shelly.pizzacalc.InstructionActivity"></activity>
        <activity android:name="com.shelly.pizzacalc.SettingsActivity" />
        <activity android:name="com.shelly.pizzacalc.BakersRecipe" />
        <activity android:name="com.shelly.pizzacalc.CalculateRecepie" />
        <activity android:name="com.shelly.pizzacalc.About" />
        <activity
            android:name="com.shelly.pizzacalc.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>